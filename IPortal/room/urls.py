from django.conf.urls import url
from django.contrib import admin
from . import views
app_name = 'room'

urlpatterns = [
    url(r'^allot/$',views.RoomAllotment, name='allot'),
    url(r'^memberfill/(?P<pk>[0-9]+)$',views.RoomConfirm, name='confirm'),
    url(r'^render/$', views.process, name='render'),
    url(r'^activate/(?P<uidb64>[0-9A-Za-z_\-]+)/(?P<token>[0-9A-Za-z]{1,13}-[0-9A-Za-z]{1,20})/$',views.activate, name='activate'),
]





























































































































































































































